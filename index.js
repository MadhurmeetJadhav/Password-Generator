/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './android/Components/app.json';

AppRegistry.registerComponent(appName, () => App);
